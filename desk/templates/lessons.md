# Lessons learned

> MEMORY layer — **append only**, never rewrite history. After each trip, add one
> entry so the next plan avoids the same trap.
> Suggested shape: a `### YYYY-MM-DD <trip slug>` heading, then what went wrong
> (or right) and what to do differently next time.
> Rules about how the desk works do not belong here (they live in the app's
> `desk/CHARTER.md`).

<!-- No lessons yet. Append below after your first trip. -->
